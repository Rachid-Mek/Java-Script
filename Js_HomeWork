var arr = [-3, 8, 7, 6, 5, -4, 3, 2, 1];

for (let i = 0; i < arr.length; i++)
  for (let j = i + 1; j < arr.length; j++)
    if (arr[j] < arr[i]) {
      let x = arr[i];
      arr[i] = arr[j];
      arr[j] = x;
    }

console.log(arr);
